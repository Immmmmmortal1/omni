// Template for local secrets. Copy to `Secrets.xcconfig` and fill in your key.
//   cp Secrets.example.xcconfig Secrets.xcconfig
// Then paste your DeepSeek API key and run `xcodegen generate`.
// Secrets.xcconfig is gitignored; this example file is safe to commit.
DEEPSEEK_API_KEY =
DEEPSEEK_MODEL = deepseek-v4-flash
